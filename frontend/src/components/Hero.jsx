export default function Hero() {
  return (
    <section className="intro">
      <h1 className="intro__title">Thousands of rows, categorized in a minute.</h1>
      <p className="intro__sub">
        Drop in a CSV. ClassifyAI reads every row, places it in the right
        category, and shows its reasoning for each one.
      </p>
    </section>
  );
}
